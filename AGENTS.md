
- 从官网采用模板构建：


```
npm create tauri-app@latest
```

- 采用 Vite 模板构建：

```

```

- 采用 TytpeScript + Rust 的技术路线


- React 前端先安装依赖，不重复造车轮，所需依赖：

    - motion
    - 路由库
    - 图标库

- 禁用浏览器右键，采用自定义菜单栏
- 禁用滑动条
- 采用自定义窗口样式，去掉默认的标题栏
